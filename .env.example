# Environment variables for Railway deployment
# Copy this file to .env.local for local development or add these in Railway dashboard

# Node environment (Railway sets this automatically, but listed for reference)
NODE_ENV=production

# Port (Railway sets PORT automatically, Next.js will use it)
PORT=3000
